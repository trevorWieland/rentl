[project]
name = "rentl-server"
version = "0.1.0"
description = "rentl server placeholder"
requires-python = ">=3.13"
dependencies = [
    "rentl-pipelines",
]

[build-system]
requires = ["uv_build>=0.9.2,<0.10.0"]
build-backend = "uv_build"
