spec_id: s0.1.49
issue: https://github.com/trevorWieland/rentl/issues/144
version: v0.1

# Plan: Multi-Model Verification & Compatibility

## Decision Record
rentl's v0.1 promise includes BYOK model support, but "support" is meaningless without verification. This spec creates a systematic way to verify that specific models work through the full pipeline, with both a CLI tool for ad-hoc checks and a test suite for regression safety. The registry is data-driven so expanding model coverage is a config edit, not a code change.

## Tasks
- [x] Task 1: Save Spec Documentation
  - Write spec.md, plan.md, demo.md, standards.md, references.md
  - Commit and push on issue branch
- [x] Task 2: Create verified-models registry schema and file
  - Define Pydantic schema for registry entries: model_id, endpoint_type (local/openrouter), endpoint_ref, config overrides (timeout, temperature, etc.), load_endpoint for local models
  - Create TOML registry file with all 9 models (4 local + 5 OpenRouter)
  - Add unit tests for schema validation and TOML loading
  - Files: `packages/rentl-schemas/src/rentl_schemas/compatibility.py`, registry TOML location, `tests/unit/schemas/test_compatibility.py`
  - Acceptance: schema loads and validates all 9 entries; invalid entries rejected
  - [x] Fix: Handle non-string/null `endpoint_type` inputs in validator without raising raw `AttributeError`; reject with a Pydantic `ValidationError` path instead (`packages/rentl-schemas/src/rentl_schemas/compatibility.py:56-59`) (audit round 1)
  - [x] Fix: Add unit coverage for `endpoint_type=None` (and/or non-string) to confirm invalid entries are rejected via validation errors (`tests/unit/schemas/test_compatibility.py`) (audit round 1)
  - [x] Fix: Remove `object` annotation from `_coerce_endpoint_type` validator signature and use explicit non-`Any` typing to satisfy `strict-typing-enforcement` (`packages/rentl-schemas/src/rentl_schemas/compatibility.py:58`) (audit round 2)
- [x] Task 3: Build shared verification runner with model loading
  - Core logic that takes a registry entry, resolves the endpoint, and runs a mini 5-phase pipeline on golden input data
  - For local models: call LM Studio load API (`http://192.168.1.23:1234/api/v1/models/load`) to switch active model before verification
  - Return structured pass/fail results per phase with error details
  - Sequential execution for local models (one at a time); parallel-safe for OpenRouter
  - Files: `packages/rentl-core/src/rentl_core/compatibility/` (runner, loader, types)
  - Tests: unit tests with mocked LLM calls + mocked load API
  - Acceptance: runner produces correct results for mock scenarios; load API integration works
  - [x] Fix: Preserve explicit zero-valued config overrides (`temperature=0.0`, `top_p=0.0`) by using `is not None` checks instead of truthy `or` fallbacks in `verify_model` (`packages/rentl-core/src/rentl_core/compatibility/runner.py:261-264`) (audit round 1)
  - [x] Fix: Replace `object` type annotations in the `_side_effect` test helper with explicit concrete types to satisfy `strict-typing-enforcement` (`tests/unit/core/compatibility/test_runner.py:126-127`) (audit round 1)
- [x] Task 4: Add `rentl verify-models` CLI command
  - Thin CLI adapter that reads the registry, invokes the shared runner, displays results
  - Supports `--endpoint` filter (local/openrouter/all) and `--model` filter
  - Reports per-model, per-phase pass/fail with actionable error messages
  - Files: `packages/rentl-cli/src/rentl_cli/commands/verify_models.py`
  - Tests: unit tests for CLI argument parsing, output formatting, wiring to shared runner
  - Acceptance: `rentl verify-models --help` works; output is clear and actionable
  - [x] Fix: Wrap `asyncio.run(verify_registry(...))` in CLI error handling so unexpected verifier/runtime exceptions return actionable CLI output instead of propagating uncaught (`services/rentl-cli/src/rentl/main.py:3885-3892`) (audit round 1, see signposts.md: Task 4 unresolved verifier exception handling)
  - [x] Fix: Add unit tests for output formatting paths (TTY rich table + non-TTY JSON emission) to satisfy Task 4 test requirements (`tests/unit/cli/test_verify_models.py`) (audit round 1)
- [x] Task 5: Add pytest compatibility test suite
  - Quality-tier tests parameterized from the verified-models registry
  - BDD-style (Given/When/Then)
  - Loads local models via LM Studio API before each test — no skipping
  - Uses the same shared runner from Task 3
  - Files: `tests/quality/compatibility/test_model_compatibility.py`, `tests/quality/compatibility/conftest.py`
  - Acceptance: `make quality` runs all compatibility tests; zero skips
  - [x] Fix: Replace the current `pytest_generate_tests` approach with a real `model_entry` fixture parametrization that pytest-bdd can resolve, so the scenario no longer errors with `fixture 'model_entry' not found` (`tests/quality/compatibility/test_model_compatibility.py:43-50`, `tests/quality/compatibility/test_model_compatibility.py:76-78`) (audit round 1)
  - [x] Fix: Verify collection produces one compatibility test instance per registry model (9 expected from the bundled registry), not a single unparameterized scenario (`LM_STUDIO_API_KEY=dummy RENTL_OPENROUTER_API_KEY=dummy pytest -q tests/quality/compatibility/test_model_compatibility.py --collect-only` currently reports `collected 1 item`) (audit round 1)
- [x] Task 6: Verify all target models pass and fix provider issues
  - Run CLI and test suite against all 9 models (4 local, 5 OpenRouter)
  - Fix any structured output or tool calling issues in provider handling — generically, no model-specific branches
  - Grep source code to confirm no hardcoded model name strings outside registry/fixtures
  - Acceptance: all 9 models pass; grep returns zero model-name hits in source
  - [x] Fix: Add schema regression tests for `config_overrides.max_output_retries` and `config_overrides.supports_tool_choice_required` (defaults/validation and bundled TOML parse behavior) to cover new Task 6 fields (`packages/rentl-schemas/src/rentl_schemas/compatibility.py:32-43`, `tests/unit/schemas/test_compatibility.py`) (audit round 1; see signposts.md: Task 6 provider/tool-choice and retry fixes)
  - [x] Fix: Add runner tests that assert Task 6 overrides are wired through `verify_model`: `supports_tool_choice_required` must be forwarded to `create_model(...)`, and `max_output_retries` must flow to `Agent(..., output_retries=...)` (`packages/rentl-core/src/rentl_core/compatibility/runner.py:283-321`, `tests/unit/core/compatibility/test_runner.py`) (audit round 1; see signposts.md: Task 6 provider/tool-choice and retry fixes)
  - [x] Fix: Add provider-factory tests that verify `supports_tool_choice_required=False` is applied to `OpenAIModelProfile.openai_supports_tool_choice_required` for both OpenRouter and generic OpenAI model creation paths (`packages/rentl-llm/src/rentl_llm/provider_factory.py:437-440`, `packages/rentl-llm/src/rentl_llm/provider_factory.py:488-491`, `tests/unit/llm/test_provider_factory.py`) (audit round 1; see signposts.md: Task 6 provider/tool-choice fix)
  - [x] Fix: [Gate] Compatibility verification uses per-call timeouts and retry settings that exceed the quality-tier 30s budget: local/OpenRouter entries set `timeout_s` to 120-180s and `verify_model` runs five sequential phases with up to four output retries, so tests hit pytest-timeout before phase-level diagnostics are returned (`packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:25-26`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:75`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:83`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:91-92`, `packages/rentl-core/src/rentl_core/compatibility/runner.py:254`, `packages/rentl-core/src/rentl_core/compatibility/runner.py:290-329`) (gate triage round 1)
  - [x] Fix: [Gate] Verified-model overrides are stale against current provider behavior: compatibility now fails in-context structured output for `qwen/qwen3.5-35b-a3b` ("Exceeded maximum retries (4) for output validation") and multiple other models time out in the same scenario; re-verify each declared model and update declarative overrides/model set without model-specific branching (`tests/quality/compatibility/test_model_compatibility.py:93-104`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:38-45`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:70-100`) (gate triage round 1, see signposts.md: Task 6 provider/tool-choice and retry fixes)
  - [x] Fix: [Gate] Pipeline quality config leaves output-validation retries at runtime defaults, which amplifies translate-phase runtime past the 30s test budget; set explicit retry bounds for the generated quality config so translate/export cannot loop until pytest-timeout (`tests/quality/pipeline/test_golden_script_pipeline.py:155-158`, `packages/rentl-agents/src/rentl_agents/runtime.py:176-181`, `packages/rentl-agents/src/rentl_agents/wiring.py:150-153`, `packages/rentl-agents/src/rentl_agents/wiring.py:1634-1637`) (gate triage round 1)
  - [x] Fix: [Gate] Quality-tier runtime budgets are still overcommitted in live failure paths: pretranslation harness allows up to six model requests at 8s each (`tests/quality/agents/quality_harness.py:81-84`) and compatibility verification still runs five sequential phases with per-phase retries (`packages/rentl-core/src/rentl_core/compatibility/runner.py:290-329`) against `timeout_s=5.0`, `max_output_retries=1` overrides (`packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:30-31`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:73-74`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:82-83`), so tests breach the 30s limit before assertions (`tests/quality/agents/test_pretranslation_agent.py:166`, `tests/quality/compatibility/test_model_compatibility.py:93`) (gate triage round 2, see signposts.md: Task 6 quality timeout budget drift)
  - [x] Fix: [Gate] Local-model loading is coupled to the same `timeout_s` override used for per-phase inference. Task 6 reduced local registry `timeout_s` to `5.0` (`packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:31`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:41`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:51`), and `verify_model` forwards that value directly into `load_lm_studio_model(..., timeout_s=entry.config_overrides.timeout_s or 120.0)` (`packages/rentl-core/src/rentl_core/compatibility/runner.py:254`). Quality gate now fails local models at context with `Model loading failed: Failed to reach LM Studio .../api/v1/models/load` (`tests/quality/compatibility/test_model_compatibility.py:101`). Decouple LM Studio load timeout/lifecycle behavior from per-phase inference timeout and add regression coverage for slow local model loads. (gate triage round 3, see signposts.md: Task 6 quality timeout budget drift; Task 6 local load-timeout coupling)
  - [x] Fix: [Gate] OpenRouter compatibility overrides are stale again after forcing `max_output_retries=0` across the registry (`packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:65`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:75`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:84`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:93`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:102`). The runner passes this directly into `Agent(..., output_retries=output_retries)` (`packages/rentl-core/src/rentl_core/compatibility/runner.py:197-201`, `packages/rentl-core/src/rentl_core/compatibility/runner.py:290-293`), and the gate now reports `Exceeded maximum retries (0)` failures (`qwen/qwen3.5-27b` context, `openai/gpt-oss-120b` qa) plus quality timeouts while still inside `when_run_verification` for other OpenRouter models (`tests/quality/compatibility/test_model_compatibility.py:93-104`). Re-verify each OpenRouter model and update declarative per-model retry/tool-choice overrides or registry membership without adding model-specific branching in source code. (gate triage round 3, see signposts.md: Task 6 quality timeout budget drift; Task 6 zero-retry compatibility drift)
  - [x] Fix: [Gate] Compatibility verification does not stop after the first failed phase, so failing models continue running the remaining phase calls and frequently hit pytest's 30s quality timeout before assertions surface the actual failing phase. Bound post-failure work by short-circuiting phase execution (or equivalent deterministic fail-fast behavior) when a phase returns `FAILED` (`packages/rentl-core/src/rentl_core/compatibility/runner.py:325-337`, `tests/quality/compatibility/test_model_compatibility.py:93`, `pyproject.toml:73`) (gate triage round 4, see signposts.md: Task 6 (gate triage round 2), quality timeout budget drift)
  - [x] Fix: [Gate] Verified-model retry/timeout overrides are stale again against live provider behavior: local entries still force `max_output_retries = 0` (matching gate errors `Exceeded maximum retries (0)`), while OpenRouter entries are tuned to an exact worst-case `5 × 2 × 3 = 30s` budget with no overhead headroom, producing recurrent timeout kills in `when_run_verification`. Re-verify every declared model and update declarative per-model retry/timeout overrides (or registry membership) so all listed models pass within the 30s quality budget without model-specific branching (`packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:17-19`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:31-35`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:67-69`, `packages/rentl-core/src/rentl_core/compatibility/runner.py:297-301`, `tests/quality/compatibility/test_model_compatibility.py:101`) (gate triage round 4, see signposts.md: Task 6 (gate triage round 2), quality timeout budget drift; Task 6 (gate triage round 3), zero-retry compatibility drift)
  - [x] Fix: [Gate] Pretranslation quality harness currently sets `max_output_retries = 0`, which collapses `_max_chunk_attempts` to one attempt and disables the pretranslation alignment-feedback recovery loop; duplicate output IDs (`Duplicate: line_1`) now fail immediately instead of retrying with corrective feedback, so rebalance quality-harness retry/request settings to preserve one corrective retry while staying within the 30s tier budget (`tests/quality/agents/quality_harness.py:83`, `packages/rentl-agents/src/rentl_agents/wiring.py:150`, `packages/rentl-agents/src/rentl_agents/wiring.py:418-469`, `tests/quality/agents/test_pretranslation_agent.py:173`) (gate triage round 5, see signposts.md: Task 6 (gate triage round 2), quality timeout budget drift)
  - [x] Fix: [Gate] Verified-model compatibility overrides are stale again against live provider behavior: with current declarative settings, local `google/gemma-3-27b` fails context output validation (`Exceeded maximum retries (1)`) and multiple OpenRouter entries time out inside `when_run_verification`; re-verify each declared model and retune per-model timeout/retry overrides (or registry membership) with explicit 30s headroom instead of edge-budget assumptions (`tests/quality/compatibility/test_model_compatibility.py:93-104`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:33-35`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:69-107`, `packages/rentl-core/src/rentl_core/compatibility/runner.py:297-348`, `pyproject.toml:73`) (gate triage round 5, see signposts.md: Task 6 (gate triage round 4))
  - [x] Fix: [Gate] Compatibility timeout budgeting still underestimates OpenRouter retry amplification: `verify_model` applies registry `max_output_retries=1` to phase agents (`packages/rentl-core/src/rentl_core/compatibility/runner.py:197-203`, `packages/rentl-core/src/rentl_core/compatibility/runner.py:297-345`) while OpenRouter models are built through `OpenRouterProvider(api_key=...)` without explicit SDK retry caps (`packages/rentl-llm/src/rentl_llm/provider_factory.py:436`), and the gate now times out `minimax/minimax-m2.5` inside `when_run_verification` before assertions run (`tests/quality/compatibility/test_model_compatibility.py:93`, gate output `Failed: Timeout (>30.0s) from pytest-timeout`). Add deterministic compatibility-path retry/time-budget controls (including OpenRouter client retry caps) with regression coverage, then re-verify the minimax registry entry under the 30s quality budget. (gate triage round 6, see signposts.md: Task 6 (gate triage round 5))
  - [x] Fix: [Gate] Quality pretranslation eval timing budget is still nondeterministic because quality harness paths do not cap SDK-level transport retries: both the profile-agent path and LLM-judge path call `create_model(...)` without `max_retries`, so default HTTP retries can multiply each 5s request and push `dataset.evaluate(...)` past pytest's 30s timeout. Add explicit SDK retry controls in quality-agent runtime/judge wiring and set deterministic values for quality runs. (`tests/quality/agents/test_pretranslation_agent.py:166`, `tests/quality/agents/quality_harness.py:81-85`, `tests/quality/agents/quality_harness.py:99-106`, `packages/rentl-agents/src/rentl_agents/runtime.py:598-608`, `packages/rentl-llm/src/rentl_llm/provider_factory.py:85-86`) (gate triage round 7)
  - [x] Fix: [Gate] Verified-model compatibility overrides are stale again against live provider behavior: the registry still declares `timeout_s=2.0` and `max_output_retries=1` for `openai/gpt-oss-20b`, `openai/gpt-oss-120b`, and `minimax/minimax-m2.5`, and `verify_model` applies those values directly, but the gate now fails QA with `Exceeded maximum retries (1)` and translate with `Request timed out`. Re-verify each declared model and retune declarative per-model retry/timeout settings (or registry membership) with explicit 30s headroom, without model-specific branching in source. (`tests/quality/compatibility/test_model_compatibility.py:101`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:49-51`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:64-66`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:74-76`, `packages/rentl-core/src/rentl_core/compatibility/runner.py:297-323`) (gate triage round 7, see signposts.md: Task 6 (gate triage round 5); Task 6 (gate triage round 6))
  - [x] Fix: [Gate] OpenRouter verified-model declarations are stale against current provider behavior: `openai/gpt-oss-120b` now fails edit with `UnexpectedModelBehavior: Invalid response from openrouter chat completions endpoint` (tool call fields null in `_OpenRouterChatCompletion` validation), while `minimax/minimax-m2.5` fails pretranslation with `ModelAPIError: Request timed out`. The current registry keeps both entries at `timeout_s=3.0`, `max_output_retries=1`, `max_sdk_retries=0` and no explicit `supports_tool_choice_required` override, and `verify_model` forwards those defaults directly (`supports_tool_choice_required=True` when unset). Re-verify both OpenRouter entries and update declarative compatibility controls or registry membership (no model-specific branching), with regression coverage for malformed OpenRouter tool-call payload handling and pretranslation timeout behavior. (`tests/quality/compatibility/test_model_compatibility.py:101`, `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml:58-76`, `packages/rentl-core/src/rentl_core/compatibility/runner.py:297-307`, `packages/rentl-core/src/rentl_core/compatibility/runner.py:310-323`, `.venv/lib/python3.14/site-packages/pydantic_ai/models/openrouter.py:567-568`, `.venv/lib/python3.14/site-packages/pydantic_ai/models/openai.py:780-782`) (gate triage round 8, see signposts.md: Task 6 (gate triage round 7))
- [x] Task 7: Resource-aware LM Studio model lifecycle management
  - The model loader only loads models and never unloads them. The GPU supports a single model at a time; additional models spill into system RAM, gradually exhausting memory and causing system instability when verifying multiple local models.
  - Add `unload_lm_studio_model()` to `loader.py` — POST to LM Studio `/api/v1/models/unload` with `{"instance_id": model_id}`
  - Add `list_lm_studio_models()` to `loader.py` — GET `/api/v1/models/list` to query currently loaded models
  - Refactor `load_lm_studio_model()` to be resource-aware: query loaded models first, skip load if target is already active, unload existing models before loading a new one
  - Add `try/finally` cleanup in `verify_model()` (runner.py) so the model is always unloaded after verification completes or fails
  - Consider an async context manager (`LMStudioModelSession`) for clean load/unload lifecycle
  - Add unit tests for unload, list, resource-aware load logic, and verify_model cleanup
  - Files: `packages/rentl-core/src/rentl_core/compatibility/loader.py`, `packages/rentl-core/src/rentl_core/compatibility/runner.py`, `tests/unit/core/compatibility/test_loader.py`
  - Acceptance: running `rentl verify-models --endpoint local` across all 4 models leaves zero models loaded after completion; at no point during the run are multiple models loaded simultaneously; unit tests cover load-skip, pre-unload, post-cleanup, and error-during-unload paths
  - See signposts.md: Task 7 (LM Studio model memory leak)
  - [x] Fix: Treat unload failures in `load_lm_studio_model` as hard failures instead of warning-only continuation/return, so local verification cannot proceed while multiple models remain loaded (`packages/rentl-core/src/rentl_core/compatibility/loader.py:172-188`) (audit round 1; see signposts.md: Task 7 unload-failure cleanup gap)
  - [x] Fix: Ensure `verify_model` local cleanup runs even when `load_lm_studio_model` raises `ModelLoadError` by restructuring the local load + phase execution path into one cleanup-guarded flow (`packages/rentl-core/src/rentl_core/compatibility/runner.py:247-266`, `packages/rentl-core/src/rentl_core/compatibility/runner.py:340-354`, `tests/unit/core/compatibility/test_runner.py:223-250`) (audit round 1; see signposts.md: Task 7 unload-failure cleanup gap)
  - [x] Fix: Add unit tests for unload-failure paths in resource-aware load logic (target-already-loaded branch and pre-load unload branch) to assert fail-fast behavior and prevent multi-model residency (`tests/unit/core/compatibility/test_loader.py`) (audit round 1; see signposts.md: Task 7 unload-failure cleanup gap)
  - [x] Fix: Make `load_lm_studio_model` fail-fast when `list_lm_studio_models` fails instead of proceeding to a blind load; this branch currently bypasses pre-unload and can violate single-model residency acceptance (`packages/rentl-core/src/rentl_core/compatibility/loader.py:149-158`) (audit round 3)
  - [x] Fix: Replace `test_load_model_proceeds_when_list_fails` with assertions that list failure returns `ModelLoadError` and does not POST `/load`, so tests enforce the no-multiple-loaded-models contract (`tests/unit/core/compatibility/test_loader.py:217-242`) (audit round 3)
  - [x] Fix: [Gate] LM Studio list-endpoint assumption is incompatible with the running server: `list_lm_studio_models` derives `GET .../api/v1/models/list` from `load_endpoint` (`packages/rentl-core/src/rentl_core/compatibility/loader.py:64`), but quality gate responses are HTTP 404 `"Unexpected endpoint or method. (GET /api/v1/models/list)"`, and fail-fast loading then aborts every local model before phase execution (`packages/rentl-core/src/rentl_core/compatibility/loader.py:149-159`, `tests/quality/compatibility/test_model_compatibility.py:101`) (gate triage round 1, see signposts.md: Task 7 LM Studio endpoint contract drift)
- [x] Task 8: Add lm-studio endpoint to rentl.toml multi-endpoint config
  - Switch `rentl.toml` from legacy single-endpoint `[endpoint]` to multi-endpoint `[endpoints]` mode
  - Add `lm-studio` endpoint entry with `provider_name = "lm-studio"`, `base_url = "http://192.168.1.23:1234/v1"`, `api_key_env = "RENTL_LOCAL_API_KEY"`, `timeout_s = 180.0`
  - Keep existing OpenRouter endpoint as a second entry
  - Set `default = "openrouter"` (preserve current default behavior)
  - Verify `rentl verify-models --endpoint local --model qwen/qwen3-vl-30b` resolves the endpoint and runs verification
  - Add integration test that loads the shipped `rentl.toml` and confirms both endpoint refs (`lm-studio`, `openrouter`) resolve
  - Files: `rentl.toml`, optionally `tests/integration/` for config validation
  - Acceptance: `rentl verify-models --endpoint local` resolves the lm-studio endpoint and runs verification against LM Studio
  - See signposts.md: Demo (run 1) — CLI endpoint resolution gap
- [ ] Task 9: Restore all 9 verified models and fix test infrastructure
  - The 7 models removed through gate triage rounds 1-8 work correctly when tested directly (curl). The removals were caused by test infrastructure problems (quality timeout budget, not model incompatibilities). All 9 models must be restored per spec acceptance criteria.
  - **Sub-task 9a: Split compatibility tests into per-phase test cases**
    - Restructure `tests/quality/compatibility/test_model_compatibility.py` from 1 test per model (running all 5 phases sequentially) to 1 test per model per phase (9 models × 5 phases = 45 test cases)
    - Each per-phase test runs a single LLM call, easily fitting within the 30s quality timeout
    - Update the BDD feature file and step definitions accordingly
    - Files: `tests/quality/compatibility/test_model_compatibility.py`, `tests/quality/compatibility/conftest.py`, `tests/features/compatibility/model_compatibility.feature`
    - Acceptance: `pytest --collect-only tests/quality/compatibility/` shows 45 test cases (9 models × 5 phases); each test completes within 30s
  - **Sub-task 9b: Restore all 9 models to the verified-models registry**
    - Re-add the 7 removed models to `verified_models.toml`: `google/gemma-3-27b`, `qwen/qwen3.5-35b-a3b` (local), `qwen/qwen3.5-27b`, `deepseek/deepseek-v3.2`, `z-ai/glm-5`, `openai/gpt-oss-120b`, `minimax/minimax-m2.5` (OpenRouter)
    - Set appropriate per-model config overrides (timeout_s, max_output_retries, supports_tool_choice_required, max_sdk_retries) with realistic values now that tests are per-phase (budget: 1 phase × (1+retries) × timeout_s ≤ 30s)
    - For models that need `supports_tool_choice_required = false`, set it declaratively
    - Update unit tests to reflect 9-model registry (4 local + 5 OpenRouter)
    - Files: `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml`, `tests/unit/schemas/test_compatibility.py`
    - Acceptance: registry contains all 9 models; unit tests pass
  - **Sub-task 9c: Investigate and fix pydantic-ai tool-call handling for OpenRouter models**
    - The signpost claims `openai/gpt-oss-120b` returns malformed tool-call payloads, but the model works correctly via curl. Investigate whether the issue is in pydantic-ai's request construction (e.g., sending parameters the model doesn't expect) or response parsing (being too strict about optional fields)
    - If the issue is in pydantic-ai internals: work around via declarative config or provider-layer tolerance, NOT model removal
    - If the issue was transient/stale: verify it's resolved and tune config overrides accordingly
    - Files: `packages/rentl-llm/src/rentl_llm/provider_factory.py`, `packages/rentl-core/src/rentl_core/compatibility/runner.py`
    - Acceptance: `openai/gpt-oss-120b` passes all 5 phases in the per-phase test suite
  - **Sub-task 9d: Verify all 9 models pass and tune config overrides**
    - Run `rentl verify-models` against all 9 models (4 local, 5 OpenRouter)
    - For any failing model, investigate the actual error (not the previous signpost claims) and fix through declarative config or generic provider handling
    - Grep source for hardcoded model names to confirm no model-specific branching was introduced
    - Files: `packages/rentl-schemas/src/rentl_schemas/data/verified_models.toml`
    - Acceptance: all 9 models pass all 5 phases; no model-specific branching in source
  - See signposts.md: Blocker resolution — Model removal reversal
