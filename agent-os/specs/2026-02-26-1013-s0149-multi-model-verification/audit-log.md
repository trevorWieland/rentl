# Audit Log

Running record of all task audits, demo runs, and spec audits.
Future auditors: check this log for regressions and patterns.

---

- **Task 2** (round 1): FAIL — `endpoint_type` validation path raises raw `AttributeError` for null/non-string input (`packages/rentl-schemas/src/rentl_schemas/compatibility.py:56-59`) instead of returning a structured `ValidationError`.
- **Task 2** (round 2): FAIL — `object` type annotation remains in `_coerce_endpoint_type`, violating `strict-typing-enforcement` (`packages/rentl-schemas/src/rentl_schemas/compatibility.py:58`).
- **Task 1** (round 1): PASS — Spec documentation bundle is complete in commit `1ea0875` (`spec.md`, `plan.md`, `demo.md`, `standards.md`, `references.md`) and matches the Task 1 deliverables.
- **Task 3** (round 1): FAIL — `verify_model` drops valid zero-valued overrides via truthy fallback logic, and Task 3 tests still use forbidden `object` type annotations.
- **Task 2** (round 3): PASS — Task 2 schema/registry implementation is compliant; resolved signposts were verified in code and `tests/unit/schemas/test_compatibility.py` passes (21/21).
- **Task 4** (round 1): FAIL — `verify-models` does not handle unexpected verifier exceptions at the CLI boundary and is missing output-formatting coverage promised by Task 4 tests.
- **Task 4** (round 2): PASS — CLI runtime exception handling and output-formatting coverage are implemented and verified; `tests/unit/cli/test_verify_models.py` passes (12/12).
- **Task 5** (round 1): FAIL — Compatibility quality test is not registry-parameterized in practice; pytest collects one scenario and fails with `fixture 'model_entry' not found`.
- **Task 5** (round 2): PASS — Registry-driven parametrization is fixed and verified; `pytest --collect-only` now discovers 9 per-model compatibility scenarios with no missing `model_entry` fixture errors.
- **Task 6** (round 1): FAIL — Task 6 fixes are implemented, but the new override wiring (`supports_tool_choice_required`, `max_output_retries`) lacks direct regression coverage in schema, runner, and provider-factory tests.
- **Task 7** (round 1): FAIL — LM Studio lifecycle guarantees break on unload/load-failure paths: pre-load unload errors are warning-only and load failures return before the cleanup `finally` runs.
- **Task 7** (round 2): PASS — Fail-fast unload handling and cleanup-guarded local verification are implemented and verified in `load_lm_studio_model`/`verify_model`, including unload-failure and load-failure cleanup tests.
- **Gate triage** (round 1): test failures — compatibility verification now exceeds quality-tier timing budget for multiple "verified" models (timeouts + context retry exhaustion), and pipeline quality translate run has retry amplification that can consume the full 30s limit before completion.
- **Task 7** (round 3): FAIL — `load_lm_studio_model` still proceeds with `/load` when model listing fails, so Task 7 single-model residency is not guaranteed (`packages/rentl-core/src/rentl_core/compatibility/loader.py:149-158`, `tests/unit/core/compatibility/test_loader.py:217-242`).
- **Gate triage** (round 2): test failures — LM Studio local-model list endpoint contract drift (`GET /api/v1/models/list` returns 404) and quality request budgets still exceed the 30s cap in pretranslation + compatibility failure paths.
- **Task 5** (round 3): PASS — Registry-driven BDD parametrization remains correct; compatibility collect-only reports one scenario per registry model (`verified_models.toml`: 8 models, `pytest --collect-only`: 8 items) with no `model_entry` fixture errors.
- **Task 7** (round 4): PASS — LM Studio listing now uses `GET /api/v1/models` with loaded-instance parsing in `list_lm_studio_models`, resolving local verification startup failures; verified by `pytest -q tests/unit/core/compatibility/test_loader.py` (17/17).
- **Gate triage** (round 3): test failures — Task 6 timeout-budget tuning regressed compatibility by coupling LM Studio load to the 5s per-phase timeout and by forcing zero output retries across OpenRouter models that no longer pass structured output reliably.
- **Task 7** (round 5): PASS — Resource-aware LM Studio lifecycle remains compliant (v1 model listing, fail-fast residency guarantees, and verify-time cleanup); verified with `pytest -q tests/unit/core/compatibility/test_loader.py tests/unit/core/compatibility/test_runner.py` (33/33).
- **Gate triage** (round 4): test failures — Task 6 compatibility drift recurred: stale retry/timeout overrides plus non-short-circuit phase execution now produce mixed structured-output failures and pytest-timeout masking in quality compatibility tests.
- **Task 7** (round 6): PASS — Latest Task 7 fix commit (`9a92e2a`) correctly targets LM Studio v1 model listing (`GET /api/v1/models`) with loaded-instance parsing in `list_lm_studio_models` (`packages/rentl-core/src/rentl_core/compatibility/loader.py:52-82`); verified by `pytest -q tests/unit/core/compatibility/test_loader.py tests/unit/core/compatibility/test_runner.py` (34/34).
- **Gate triage** (round 5): test failures — Task 6 now has two root-cause regressions: pretranslation alignment recovery is disabled by `max_output_retries=0`, and compatibility registry retry/timeout overrides are stale against live provider behavior (context validation failure + OpenRouter pytest-timeout kills).
- **Task 7** (round 7): PASS — Most recent Task 7 completion commit (`9a92e2a`) remains compliant: LM Studio v1 model listing uses `GET /api/v1/models` and loaded-instance parsing in `list_lm_studio_models` (`packages/rentl-core/src/rentl_core/compatibility/loader.py:52-82`); verified by `pytest -q tests/unit/core/compatibility/test_loader.py tests/unit/core/compatibility/test_runner.py` (34/34).
- **Gate triage** (round 6): test failures — Task 6 compatibility timing still regresses because OpenRouter SDK retries amplify per-phase runtime beyond the 30s quality timeout despite `timeout_s=2.0`/`max_output_retries=1` registry settings.
- **Task 7** (round 8): PASS — Latest completed Task 7 implementation remains compliant with resource-aware LM Studio lifecycle requirements; verified by `pytest -q tests/unit/core/compatibility/test_loader.py` (17/17) and `pytest -q tests/unit/core/compatibility/test_runner.py` (19/19).
- **Gate triage** (round 7): test failures — Task 6 regressed in two roots: quality pretranslation still exceeds 30s because quality harness paths leave SDK retries at defaults, and compatibility registry overrides drifted again (`gpt-oss-20b`/`gpt-oss-120b` QA retry exhaustion + `minimax` translate timeout).
- **Task 7** (round 9): PASS — Latest completed Task 7 implementation remains compliant: `load_lm_studio_model` still enforces fail-fast single-model residency (`packages/rentl-core/src/rentl_core/compatibility/loader.py:160-229`) and `verify_model` still guarantees local unload cleanup in `finally` (`packages/rentl-core/src/rentl_core/compatibility/runner.py:253-376`); verified by `pytest -q tests/unit/core/compatibility/test_loader.py tests/unit/core/compatibility/test_runner.py` (36/36).
- **Gate triage** (round 8): test failures — Task 6 OpenRouter compatibility drift recurred: `openai/gpt-oss-120b` now fails on malformed OpenRouter tool-call payload validation and `minimax/minimax-m2.5` times out in pretranslation under the current declarative overrides.
- **Task 7** (round 10): PASS — Latest completed Task 7 implementation remains compliant: LM Studio model listing uses the v1 `GET /api/v1/models` contract with loaded-instance parsing (`packages/rentl-core/src/rentl_core/compatibility/loader.py:39-84`), and resource-aware load/unload lifecycle guarantees still hold with `verify_model` cleanup; verified by `pytest -q tests/unit/core/compatibility/test_loader.py tests/unit/core/compatibility/test_runner.py` (36/36).
- **Demo** (run 1): FAIL — 4/5 [RUN] steps fail: registry has 2 models (not 9), CLI can't resolve lm-studio endpoint, no OpenRouter models to verify. Step 5 (hardcoded model grep) passes. Two root causes: (A) spec model-count acceptance criteria unachievable after 7 models removed in gate triage, (B) `rentl.toml` missing lm-studio endpoint for CLI path. Task 8 added. (5 run, 1 verified)
- **Task 8** (round 1): PASS — `rentl.toml` now uses multi-endpoint `[endpoints]` with `openrouter` + `lm-studio` and default `openrouter`; BDD integration coverage verifies both endpoint refs and registry endpoint_ref resolution (`tests/integration/cli/test_verify_models_config.py`, 2/2 passed).
- **Demo** (run 2): FAIL — Step 2 now PASS (Task 8 fixed CLI endpoint resolution), step 5 PASS (no hardcoded model branching). Steps 1/3/4 still fail: spec expects 9 models but registry has 2 after gate triage. Spec-level issue for walk-spec. (5 run, 2 verified)
- **Demo** (run 3): FAIL — Steps 2, 5 PASS (functionally correct for registered models). Steps 1, 3, 4 FAIL due to stale demo expectations encoding original 9-model count. Task 9 added to update demo.md step expectations to match actual registry. (5 run, 2 verified)
- **Task 9** (round 1): FAIL — Per-phase compatibility path bypasses LM Studio load/unload lifecycle for local models (`verify_single_phase` + fixtures), so local per-model verification is not guaranteed.
- **Task 9** (round 2): FAIL — Local lifecycle wiring was added to `model_entry`, but promised regression coverage is incomplete: new tests only assert `verify_single_phase` does not call lifecycle APIs and do not verify fixture-level load/unload cleanup on success/failure paths.
- **Gate triage** (round 9): test failures — quality timeout budgets still undercount multi-request tool/judge paths (pretranslation + pipeline), and Task 9's uniform compatibility overrides have drifted from live model behavior (context retry exhaustion + phase timeouts).
- **Task 8** (round 2): PASS — Task 8 commit `bd76599` remains compliant: shipped `rentl.toml` still defines multi-endpoint config with `openrouter` + `lm-studio` and default `openrouter`, and endpoint-ref resolution coverage passes (`pytest -q tests/integration/cli/test_verify_models_config.py`, 2/2 passed).
- **Task 8** (round 3): PASS — Most recent completed task remains compliant: shipped config still uses multi-endpoint `[endpoints]` with `openrouter` + `lm-studio` (`rentl.toml`), and integration endpoint-ref resolution checks still pass (`pytest -q tests/integration/cli/test_verify_models_config.py`, 2/2 passed).
- **Gate triage** (round 10): test failures — Task 9 compatibility overrides are stale again (retries/tool-choice drift), and both compatibility/pipeline paths still lack outer wall-clock guards so stalled model calls surface as pytest timeout kills instead of structured phase failures.
- **Task 8** (round 4): PASS — Task 8 commit `bd76599` still satisfies the task contract: shipped `rentl.toml` remains in multi-endpoint mode with `openrouter` + `lm-studio` and default `openrouter`, and `pytest -q tests/integration/cli/test_verify_models_config.py` passes (2/2).
