[project]
name = "rentl-templates"
version = "0.1.0"
description = "Project templates for rentl"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.9.2,<0.10.0"]
build-backend = "uv_build"
