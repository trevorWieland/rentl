[project]
name = "rentl-pipelines"
version = "0.1.0"
description = "Pipeline orchestration for rentl"
requires-python = ">=3.13"
dependencies = [
    "rentl-core",
    "rentl-agents"
]

[build-system]
requires = ["uv_build>=0.9.2,<0.10.0"]
build-backend = "uv_build"
