# Copy to .env and fill in with your credentials / settings

# Primary LLM (agentic reasoning and orchestration)
OPENAI_URL=http://localhost:1234 # Point to any OpenAI-style API
OPENAI_API_KEY=not-needed # Set if your OpenAI-style API needs one
LLM_MODEL=openai/gpt-oss-20b

# Machine Translation (MTL) backend (optional, for specialized translation)
MTL_URL=http://localhost:1234 # Point to any OpenAI-style API
MTL_API_KEY=not-needed # Set if your OpenAI-style API needs one
MTL_MODEL=sugoi-14b-ultra
MTL_SYSTEM_PROMPT=You are a professional localizer whose primary goal is to translate Japanese to English. You should use colloquial or slang or hentai vocabulary if it makes the translation more accurate. Always respond in English.

# Optional services
TAVILY_API_KEY=tvly-dev-*
LANGSMITH_API_KEY=
